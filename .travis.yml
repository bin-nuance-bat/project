language: node_js

node_js:
- '10'

install: npm install

script:
  - npm run lint
  - npm run build
  - npm test

deploy:
  - provider: firebase
    token: $FIREBASE_TOKEN
    skip_cleanup: true
    on:
      branch: BUG-FIX_travis-deployment
    project: honesty-store-kiosk-dev