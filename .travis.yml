language: node_js

node_js:
- '10'

deploy:
  provider: script
  script: npm install && npm run deploy-dev
  on:
    branch:
      - dev
      - config
  
  provider: script
  script: npm install && npm run deploy
  on:
    branch: master

script:
- npm install && npm test
- npm run lint
