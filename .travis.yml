language: node_js

node_js:
- '10'

deploy:
  provider: script
  script: npm install && npm run deploy
  on:
    branch: config
  project: honesty-store-kiosk-dev

script:
- npm install && npm test
- npm run lint
