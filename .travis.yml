language: node_js

node_js:
- '10'

install: npm install

script:
  - npm run lint
  - npm run build
  - npm test

deploy:
  skip_cleanup: true
  provider: firebase
  token: $FIREBASE_TOKEN
  on:
    branch: demo
  project: honesty-store-kiosk-dev