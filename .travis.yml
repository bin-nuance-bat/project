language: node_js

node_js:
- '10'

install: npm install

script:
- npm run lint
- npm test
- npm run build

before_deploy: npm install firebase-tools -g

deploy:
  skip_cleanup: true
  provider: script
  script: npm run deploy
  on:
    branch: master
    branch: config
