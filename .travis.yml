language: node_js

node_js:
  - '10'

install: npm install

script:
  - npm run lint
  - npm test
  - npm run build

deploy:
  skip_cleanup: true
  provider: firebase
  token: $FIREBASE_TOKEN
  on:
    branch: master
  project: honesty-store-kiosk